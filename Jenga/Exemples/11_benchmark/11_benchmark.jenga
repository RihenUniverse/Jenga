from Jenga import *

with workspace("BenchmarkDemo"):
    configurations(["Release", "Debug"])
    targetoses([TargetOS.WINDOWS, TargetOS.LINUX])
    targetarchs([TargetArch.X86_64])

    with project("BenchApp"):
        consoleapp()
        language("C++")
        files(["src/**.cpp"])
